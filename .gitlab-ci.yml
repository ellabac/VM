pages:
  stage: deploy
  script:
    - mkdir public
    - cp index.html public/
    - cp tippningar.xlsx public/
  artifacts:
    paths:
      - public
  rules:
    - if: '$CI_COMMIT_BRANCH == "main"'